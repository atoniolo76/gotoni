name: "my-instance-name"
provider: "lambdalabs"  # Required: "lambdalabs" or "runpod"
region: "us-west-1"     # Optional: filter to specific region

gpu:
  min_vram_gb: 24
  min_cuda_version: "11.8"

container:
  image: "nvidia/cuda:11.8-devel-ubuntu22.04"  # Your Docker image